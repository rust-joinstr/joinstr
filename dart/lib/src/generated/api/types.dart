// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `from_pool`, `native_network`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `from`, `from`, `from`, `try_from`, `try_from`

enum BitcoinNetwork {
  bitcoin,
  testnet,
  signet,
  regtest,
  ;
}

/// A spendable coin (UTXO). Round-trips losslessly back into a coinjoin input.
class FfiCoin {
  final String txid;
  final int vout;
  final BigInt valueSat;
  final Uint8List scriptPubkey;
  final int sequence;
  final int coinPathDepth;
  final int? coinPathIndex;

  const FfiCoin({
    required this.txid,
    required this.vout,
    required this.valueSat,
    required this.scriptPubkey,
    required this.sequence,
    required this.coinPathDepth,
    this.coinPathIndex,
  });

  @override
  int get hashCode =>
      txid.hashCode ^
      vout.hashCode ^
      valueSat.hashCode ^
      scriptPubkey.hashCode ^
      sequence.hashCode ^
      coinPathDepth.hashCode ^
      coinPathIndex.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FfiCoin &&
          runtimeType == other.runtimeType &&
          txid == other.txid &&
          vout == other.vout &&
          valueSat == other.valueSat &&
          scriptPubkey == other.scriptPubkey &&
          sequence == other.sequence &&
          coinPathDepth == other.coinPathDepth &&
          coinPathIndex == other.coinPathIndex;
}

/// A single peer's coinjoin parameters.
class FfiPeerConfig {
  /// BIP39 mnemonic of the wallet that owns `input` and will sign.
  final String mnemonic;

  /// Electrum server hostname / IP, without a port. Prefix with `ssl://` to
  /// negotiate TLS; anything else connects in plaintext.
  final String electrumAddress;
  final int electrumPort;

  /// The coin spent into the coinjoin (e.g. from `list_coins`).
  final FfiCoin input;

  /// Address that receives this peer's denominated output.
  final String outputAddress;

  /// Nostr relay url (`wss://` or `ws://`).
  final String relay;
  final BitcoinNetwork network;

  /// SOCKS5 proxy (`host:port`) for every relay and electrum connection this
  /// peer opens, e.g. a local Tor port such as `127.0.0.1:9050`. `None`
  /// connects directly. Each connection uses a fresh isolation credential so
  /// Tor assigns it its own circuit.
  final String? proxy;

  const FfiPeerConfig({
    required this.mnemonic,
    required this.electrumAddress,
    required this.electrumPort,
    required this.input,
    required this.outputAddress,
    required this.relay,
    required this.network,
    this.proxy,
  });

  @override
  int get hashCode =>
      mnemonic.hashCode ^
      electrumAddress.hashCode ^
      electrumPort.hashCode ^
      input.hashCode ^
      outputAddress.hashCode ^
      relay.hashCode ^
      network.hashCode ^
      proxy.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FfiPeerConfig &&
          runtimeType == other.runtimeType &&
          mnemonic == other.mnemonic &&
          electrumAddress == other.electrumAddress &&
          electrumPort == other.electrumPort &&
          input == other.input &&
          outputAddress == other.outputAddress &&
          relay == other.relay &&
          network == other.network &&
          proxy == other.proxy;
}

/// A coinjoin pool advertised on a nostr relay.
///
/// Pass `raw_json` back to `join_coinjoin` to join; the other fields are decoded
/// for display. There is deliberately no `network` field: pool events do not
/// carry one, so it cannot be known here. The caller supplies the network when
/// joining, via `FfiPeerConfig::network`.
class FfiPool {
  final String id;

  /// Canonical JSON; pass back to `join_coinjoin`.
  final String rawJson;

  /// Denomination of each output, in satoshis.
  final BigInt denominationSat;
  final int peers;

  /// When the pool expires, as a unix timestamp in seconds. Pool events carry
  /// an absolute instant (`now + max_duration`), never a duration.
  final BigInt expiresAtUnixSec;
  final String relay;

  /// Fixed fee rate in satoshis per vByte. Pools that delegate their fee to a
  /// provider cannot be joined by this version and are never listed.
  final int feeRate;

  /// Initiator's nostr public key, as hex.
  final String publicKey;
  final String? version;

  const FfiPool({
    required this.id,
    required this.rawJson,
    required this.denominationSat,
    required this.peers,
    required this.expiresAtUnixSec,
    required this.relay,
    required this.feeRate,
    required this.publicKey,
    this.version,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      rawJson.hashCode ^
      denominationSat.hashCode ^
      peers.hashCode ^
      expiresAtUnixSec.hashCode ^
      relay.hashCode ^
      feeRate.hashCode ^
      publicKey.hashCode ^
      version.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FfiPool &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          rawJson == other.rawJson &&
          denominationSat == other.denominationSat &&
          peers == other.peers &&
          expiresAtUnixSec == other.expiresAtUnixSec &&
          relay == other.relay &&
          feeRate == other.feeRate &&
          publicKey == other.publicKey &&
          version == other.version;
}

/// Configuration for a coinjoin pool this peer initiates.
class FfiPoolConfig {
  /// Denomination of each output, in BTC.
  final double denominationBtc;

  /// Fee rate, in satoshis per vByte. Each output is reduced by `fee * 100`
  /// satoshis, 100 vB being the assumed weight of one participant's
  /// input+output pair.
  final int fee;

  /// Seconds to wait for the pool to fill.
  final BigInt maxDuration;

  /// Peers required before the coinjoin is built.
  final int peers;
  final BitcoinNetwork network;

  const FfiPoolConfig({
    required this.denominationBtc,
    required this.fee,
    required this.maxDuration,
    required this.peers,
    required this.network,
  });

  @override
  int get hashCode =>
      denominationBtc.hashCode ^
      fee.hashCode ^
      maxDuration.hashCode ^
      peers.hashCode ^
      network.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FfiPoolConfig &&
          runtimeType == other.runtimeType &&
          denominationBtc == other.denominationBtc &&
          fee == other.fee &&
          maxDuration == other.maxDuration &&
          peers == other.peers &&
          network == other.network;
}
